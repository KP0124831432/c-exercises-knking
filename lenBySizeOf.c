#include <stdio.h>
#include <unistd.h>

int main()
{
  //size of string
  char string[13] = "Hola";
  
  //sizeof(a) / sizeof(a[0])

  //ex
  //
  for (i = 0; i < (int ) (sizeof(a) / sizeof(a[0])); i++)
    a[i] = 0;


}
